# DecodeLabs-Internship
1st project of python. In this i have made a simple to-do list which does the following things -
add tasks to a list,
view all tasks and
runs in a loop until you choose to exit
We can run this in python 3 or any compiler in browser 

2nd project of python. In this i have made a expense tracker where we create a script where users enter expense amount. the program then adds them up and displays the total spent.
We can run this in python 3 or any compiler in browser

3rd project of python. In this i have made a password generator which creates a password randomly using letters and alphabets. 
We can run this in python 3 or any compiler in browser
